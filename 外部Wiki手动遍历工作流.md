# 外部 Wiki 手动遍历工作流（土办法 · AI 批次作业手册）

> **目标**：让 AI **人工逐步**打开/登录目标网站的 wiki，把相关词条正文摘出来，整理成与 `lunhui-wiki` 同形态的本地百科（MkDocs + 人物/势力/地点… 分目录 md）。  
> **不做**：整站爬虫脚本、批量无头请求、绕过付费墙/反爬、自动登录破解。  
> **做**：像人一样「开页 → 读 → 摘 → 落页 → 打勾防重」，可断点续跑。

---

## 〇、开工前你必须填好的配置（缺一不可）

把下面整段复制进目标百科目录的 `_源站配置.md`，**方括号占位全部换成实值**后再开工：

```markdown
# 源站配置（本站只读·执行模型不得改本文件结构，只能按规则更新进度文件）

## 基本
- 书名/主题：【例如：某某小说 / 某某游戏】
- 本地百科目录：【例如：xxx-wiki/】
- 源站名称：【例如：萌娘百科 / 百度百科 / 某某 fandom / 站内 wiki】
- 源站入口 URL：【首页或作品总览页完整 URL】
- 是否需要登录：是 / 否
- 登录方式：账号密码 / Cookie 已由用户浏览器登录 / 无需
- 登录后可见范围：【全站 / 仅部分栏目 / 仅会员词条】

## 收录边界（防止跑偏）
- 白名单路径/栏目（只抓这些）：【如 /wiki/人物/ 、分类:角色 】
- 黑名单（永不打开）：【讨论页、编辑页、历史、用户页、特殊页、广告、外链站】
- 语言：中文 / 日文 / …
- 是否允许跨站跟链：否（默认否；仅同域）

## 单批规模
- 每批最多打开词条数：【建议 5~10；人物密的站用 3~5】
- 单页摘录上限：正文纯文字约【8000】字；更长则拆「本页摘要 + 分节摘录」，不整页灌水

## 落库形态（对齐轮回 wiki）
- 目录结构固定为：docs/{总览,剧情,人物,势力,地点,物品,设定}/
- 导航：本模板默认 **不写 mkdocs.yml nav**（目录自动生成）；若你改用 lunhui 手写 nav 模式，在本配置注明「nav 由师傅合入」
```

> **登录由用户完成**：你在浏览器里先登录好源站；AI 只使用你提供的「已登录可访问」的页面内容（你粘贴、截图 OCR、或会话内浏览器工具读到的正文）。  
> AI **禁止**索要/存储你的账号密码；禁止把 Cookie 写进仓库。

---

## 一、角色分工

| 角色 | 做什么 | 绝对禁区 |
|---|---|---|
| **学徒（执行 AI）** | 按工单打开本批 URL → 摘正文 → 归类 → 写/改本地 `docs/**/*.md` → 更新防重表 → 写交接单 | 禁改：`_源站配置.md` 结构、`mkdocs.yml`（若声明师傅负责）、`scripts/`、源站账号；禁跑爬虫/批量下载脚本；禁写训练记忆里「源站没给的」设定 |
| **师傅（强模型/你）** | 收交接 → 抽查深度与出处 → build（可选）→ 合 nav（若需要）→ 拍板存疑项 | — |
| **你** | 登录源站；发工单；必要时把「当前页正文」粘给 AI；对版权/范围拍板 | — |

---

## 二、本地目录与防重账本（核心）

在本地百科目录下固定这些文件（工单 A 建站时创建）：

```
{{wiki目录}}/
├─ _源站配置.md              ← 你填好的配置（学徒只读）
├─ _工作手册.md              ← 本文全文存一份
├─ PROGRESS.md               ← 批次进度指针
├─ _crawl/                   ← ★ 防重与队列（最重要）
│  ├─ seed-urls.md           ← 种子：总览/目录/分类页 URL 列表
│  ├─ queue.md               ← 待抓队列（未完成）
│  ├─ done.md                ← ★ 已完成词条（防重复）
│  ├─ skip.md                ← 跳过（无内容/重复别名/超边界）
│  ├─ fail.md                ← 失败（打不开/需登录/空白）
│  └─ url-map.md             ← 源站 URL ↔ 本地 md 路径映射
├─ _raw/                     ← 可选：每页原文摘录备份（纯文本）
│  └─ <slug>.txt
├─ _交接/
└─ docs/
   ├─ index.md
   ├─ 总览/  剧情/  人物/  势力/  地点/  物品/  设定/
```

### 2.1 `done.md` 格式（登录过且 wiki 写完必须记）

每完成 **一个源站词条 → 本地页**，立刻在 `done.md` **末尾追加一行**（禁止事后补记漏记）：

```markdown
| 完成时间 | 源站URL | 源站标题 | 本地路径 | 分类 | 正文去空白字数 | 批次 |
|---|---|---|---|---|---|---|
| 2026-07-25 | https://…/条目A | 条目A | docs/人物/条目A.md | 人物 | 1820 | B03 |
```

**防重判定顺序（打开任何新 URL 前必做）：**

1. 该 URL 是否已在 `done.md` / `skip.md` / `fail.md`？→ 在则 **跳过，不重写**（除非工单写「强制重抓」）。
2. 规范化 URL 后再比：去掉 `?utm_*`、`#段落`、尾部 `/`；http/https 统一。
3. 标题是否已在 `url-map.md` 映射到本地页？→ 在则只做 **增量追加**（见 §5.3），不新建重复页。
4. 别名撞车：若源站「又名 B」而本地已有 `B.md`，合并进已有页，并在 front-matter `别名` 登记；`done` 里两条 URL 可指向同一本地路径。

### 2.2 `queue.md` 格式

```markdown
# 待抓队列（从上往下取；取走后删行或标 [in-progress]）

- [ ] https://…/分类:人物          # seed
- [ ] https://…/条目X              # 从目录页发现
- [in-progress] https://…/条目Y    # 本批正在做
```

规则：

- 新发现的站内链接 → 先过白/黑名单 → 不在 done/skip/fail → **追加到 queue 末尾**。
- 每批开工只把本批要做的条目标 `[in-progress]`；交批后成功 → 移入 `done`；失败 → `fail`；决定不收 → `skip`。
- **禁止**无界 BFS 把整站塞爆；每批从 queue **最多取 N 条**（配置里的单批规模）。

### 2.3 `url-map.md`

```markdown
| 源站URL | 本地md | 状态 |
|---|---|---|
| https://…/苏晓 | docs/人物/苏晓.md | done |
```

---

## 三、内容标准（对齐轮回 wiki · 写薄=返工）

### 3.1 五条铁则

1. **忠于源页**：只写本页（及本批已打开页）明确写出的内容；推断标「（推测）」。
2. **不脑补剧情**：禁用训练记忆补全源站没写的设定/结局。
3. **增量不覆盖**：旧本地页只追加；源站修订推翻旧文时改写并注「（源站修订·日期）」。
4. **必标出处**：关键句后标源站，格式：`（源：<词条标题>）` 或 `（源：<URL 短名>）`。
5. **一个事实一个正主**：详情写在实体自己的页；index 只索引+一句话+链接。

### 3.2 人物定级与字数（正文去空白，front-matter 不计）

| 级 | 判据 | 新建下限 | 必备小节 |
|---|---|---|---|
| S | 主角/核心同伴 | 专页；每批有新信息就更新 | 当前状态 / 能力 / 成长或经历 / 关系 / 出处 |
| A | 源页信息密、跨多段、有能力表 | ≥800 字 | 简介 / 外貌性格 / 能力（逐条）/ 经历 / 关系 / 出处 |
| B | 有独立词条但信息中等 | ≥400 字 | 简介 / 能力或经历 / 关系 / 出处 |
| C | 词条极短/仅列表提及 | ≥150 字 | 简介 / 出处；可注「（源页信息有限）」 |

势力/地点/物品/设定单页建议 ≥400 字；素材不够 → 先并入合集页或 index，不硬凑。

### 3.3 经历/能力写法（禁止空话）

- **经历**：时间/地点 · 因果 · 关键动作 · 数字与专有名照抄 · 结果。（能按源页小节分段就分段。）
- **能力/装备**：名称 + 效果要点 + 品级/等级（若有）+ 出处；禁止「实力强大」。
- **列表前空行**：段落与 `- ` 列表之间必须空一行（python-markdown 铁则）。

### 3.4 front-matter 模板

**人物**

```yaml
---
title: 全名
分类: 人物
所属: 势力或作品内阵营（不详则 不详）
状态: 存活
别名: []
源站: <完整URL>
首次收录批次: B0X
---
```

**势力 / 地点 / 物品 / 设定** 同理，改 `分类`，保留 `源站` 与 `首次收录批次`。

文件名 = 实体正式名；去掉 Windows 非法字符 `<>:"/\|?*`；同名不同义用括号，如 `张三(黑风寨).md`。

---

## 四、工单 A · 首次建站（只做一次）

1. 建 `{{wiki目录}}/`，存入 `_工作手册.md`（本文）、`_源站配置.md`（你已填）。
2. 建 `mkdocs.yml`（可与 `小说百科-批次作业手册模板.md` 第四节同款 Material 主题；`site_name` 改成目标名）。
3. 建 `docs/index.md` + 七个分类 `index.md` + `总览/时间线.md`（可先空）。
4. 建 `_crawl/` 五件套空表（seed / queue / done / skip / fail / url-map）。
5. **种子播种**（你或学徒一次完成）：
   - 打开源站「作品首页 / 分类目录 / 角色列表 / 导航模板」等 **入口页**；
   - 把白名单内的词条链接写入 `seed-urls.md` 与 `queue.md`（先目录页，再词条）；
   - 不要一上来就深挖外链。
6. 写 `PROGRESS.md`：

```markdown
# 进度

> 进度指针：已完成词条 0 个；下一批从 queue 顶部取；已完成批次：0

## 批次日志
| 批次 | 处理URL数 | 新建页 | 修改页 | 交接单 |
|---|---|---|---|---|

## 备注
- 源站：见 _源站配置.md
- 防重：一切以 _crawl/done.md 为准
```

7. 交接：`_交接/工单A建站.md`。

---

## 五、工单 B · 批次循环（每批执行 · 详细步骤）

### 步 0 · 开工闸（不做完不许动笔）

1. Read：`_工作手册.md`、`_源站配置.md`、`PROGRESS.md`、`_crawl/done.md`（至少读表头+最近 30 行）、`_crawl/queue.md`。
2. 回复开头**复述**：
   - 源站名 + 本批将处理的 URL 列表（≤N）
   - 五条铁则
   - A/B/C 字数配额
   - 防重规则一句话：「done 里有的 URL 不重抓」
3. 从 `queue.md` 顶部取本批条目，标 `[in-progress]`。

### 步 1 · 打开源页（手动 / 半手动）

对队列中每一条 URL，按顺序：

1. **防重检查**（§2.1）→ 命中则从 queue 移除，记 skip 原因「已在 done」。
2. **获取正文**（三选一，工单写明用哪种）：
   - **A. 用户粘贴**：你把页面正文贴进对话；学徒只处理粘贴段。
   - **B. 浏览器工具**：会话内打开 URL，读取主内容区（见 §5.1 清洗规则）。
   - **C. 本地已存 HTML/TXT**：Read `_raw/` 或用户提供的导出文件。
3. 若打不开 / 需登录却无内容 / 验证码：
   - 写入 `fail.md`：`| URL | 原因 | 批次 |`
   - **停该条**，继续下一条；不要死循环重试超过 1 次。
4. 可选：把清洗后的纯文本备份到 `_raw/<slug>.txt`（便于返工）。

#### 5.1 正文清洗规则（摘「人话」不要整页垃圾）

**保留**：词条标题、信息框关键字段、目录下各节正文、列表、表格文字、站内相关链接标题。

**删除**：

- 导航、页眉页脚、登录注册、广告、推荐位、「编辑」「讨论」「历史」
- 无意义的「播报」「编辑」按钮文案
- 大段重复的版权页脚、分享按钮
- 仅装饰性的图片 alt 堆砌（有信息的图注可留一句）

输出内部结构建议：

```text
# 源标题
URL: …
## 信息框
- 字段: 值
## 节名1
…
## 节名2
…
## 本页链出（站内、白名单内）
- 标题 | URL
```

### 步 2 · 实体清点（先规划后写文件）

每页摘完后，在回复里输出（再动笔）：

```text
## 本页规划 · <标题>
- 主实体：<名> → 分类：人物/势力/… → 级别 A/B/C
- 动作：新建 / 增量更新 → 本地路径
- 次要实体（是否拆页）：…
- 新发现白名单链接（将入 queue）：…
- 跳过链接（黑名单/外站）：…
```

**归类启发式**（拿不准写入交接「存疑」）：

| 源页类型 | 落到 |
|---|---|
| 角色/人物 | docs/人物/ |
| 组织/门派/国家/公会 | docs/势力/ |
| 地名/副本/星球 | docs/地点/ |
| 装备/道具/材料 | docs/物品/ |
| 等级体系/规则/术语 | docs/设定/ |
| 剧情梗概/章节列表 | docs/剧情/ 或 总览/时间线 |
| 作品总览 | docs/总览/ |

一页多实体：以**页面主标题**建主页；正文中的次要具名角色 **仅当源站也有独立词条** 才拆页，否则写进主页「关系/相关」并链到以后词条。

### 步 3 · 落成本地 wiki 页

1. **新建**：front-matter + 按级别写满小节；文末：

```markdown
## 出处
- 源站词条：[标题](完整URL)（收录批次 B0X）
```

2. **增量**：先 Read 本地已有页 → 只在对应小节**追加**源页新信息；不删历史。
3. **互链**：同目录 `[名](名.md)`；跨目录 `[名](../人物/名.md)`；**只链已存在文件**（未建页的先纯文本，建页后再改链——可在本批末统一补）。
4. **时间线（可选但推荐）**：若源页有明确时间/章节顺序事件，追加到 `docs/总览/时间线.md`：  
   `- **<事件名>**：…（源：词条X）`
5. **分类 index**：新实体在对应 `docs/<分类>/index.md` 加一行索引（一句话+链接），**禁止**把详情只写在 index。

### 步 4 · 登记防重（本步与写页同批完成，禁止拖到下批）

对每个成功处理的 URL **立刻**：

1. `done.md` 追加一行（§2.1）
2. `url-map.md` 追加/更新映射
3. `queue.md` 去掉该条（或标 done 后删除）
4. 本页新发现的白名单链接 → 去重后追加 `queue.md` 末尾（**本批不继续深挖**，除非本批名额未满且工单允许「顺带 1 跳」）

### 步 5 · 自检清单（逐项勾完才交批）

- [ ] 本批每条 in-progress URL 都进入了 done / skip / fail 之一，无悬空
- [ ] done 表 URL 与本地 md 路径可对上；无重复新建同名页
- [ ] 新建人物页达到 A/B/C 字数或已注「源页信息有限」
- [ ] 无训练记忆补丁；关键句有「（源：…）」
- [ ] 旧页是追加不是整页覆盖
- [ ] 段落与列表之间有空行；文件名无非法字符
- [ ] 未把账号密码/Cookie 写入任何文件
- [ ] 未改 `_源站配置.md` 结构；未越黑名单栏目

### 步 6 · 交接单

`_交接/B0X-<简短主题>.md`：

```markdown
# 交接单 · 批次 B0X
## 1. 范围
本批处理的 URL 列表 + 获取方式（粘贴/浏览器/本地文件）
## 2. 改动
### 新建
| 本地路径 | 级别 | 字数 | 源URL |
|---|---|---|---|
### 修改
| 本地路径 | 改动摘要 |
|---|---|
## 3. 防重账本变更
- done 新增 N 行
- queue 新增 M 条（列出）
- skip / fail 列表
## 4. 存疑
归类不定、源页冲突、需登录未拿到的页
## 5. 下一批建议
queue 顶部接下来 K 条
```

### 步 7 · 更新 PROGRESS.md（只许动指针+日志表末行）

```markdown
> 进度指针：已完成词条 【总数】 个；queue 剩余约 【条】；已完成批次：X
```

日志表追加：`| B0X | N | 新建a | 修改b | _交接/….md |`

---

## 六、登录相关操作规范（土办法专用）

1. **登录只发生在用户侧浏览器**。用户登录后：
   - 把需要的词条正文复制给 AI；或
   - 在已登录会话里用浏览器工具打开链接（若环境支持）。
2. AI 若发现页面是「请登录后查看」：
   - 记入 `fail.md`，原因=`需登录`；
   - 交接单写「请用户登录后重投该 URL」；
   - **禁止**尝试爆破、验证码识别绕过、伪造身份。
3. 同一词条登录前后内容不同：以**登录后完整版**为准；若先前用公开残页建过档，本批做增量补全，并在页内注「（会员区补全·批次B0X）」。
4. 频率：人工节奏，页与页之间可暂停；不要在一分钟内连开几十页（对源站不友好，也易触发风控导致整批失败）。

---

## 七、与「轮回 wiki」形态对齐检查表

| 轮回 wiki 特征 | 本工作流对应 |
|---|---|
| 分目录 md + front-matter | §3.4 + docs 七分类 |
| 人物独立页 + 关系节 | 每具名主实体一页；关系互链 |
| 经历写细、能力逐条 | §3.2–3.3 |
| 出处章节 | 改为源站词条 URL/标题 |
| 防剧透渐进 | 只收录已打开源页；不预写未访问词条 |
| 进度与交接 | PROGRESS + `_交接/` + `_crawl/done` |
| 师傅闸口 | 抽查字数、防重表、随机回源核对 |

> 差异说明：轮回 wiki 的「通关结算快照」依赖小说章节进度；**源站镜像百科一般不需要结算快照**，除非源页本身就是「角色面板/数值页」——那时用表格忠实转录即可。

---

## 八、常见翻车

| 翻车 | 正确做法 |
|---|---|
| 不写 done 导致下批重复建页 | 每成功一条立刻记 done + url-map |
| URL 带参数被当成新页 | 规范化后再比 |
| 把整站导航/广告贴进 md | §5.1 清洗 |
| 用自己知识补全角色结局 | 只写源页有的；缺则「不详」 |
| queue 无限膨胀 | 每批最多 N 条；新链只入队不连抓 |
| 别名建了两个文件 | url-map 合并；front-matter 别名 |
| 详情只写在 index | 独立页 + index 一行索引 |
| 把 Cookie/密码写进仓库 | 绝对禁止 |
| 一页写太薄交批 | 达配额或注「源页信息有限」 |

---

## 九、工单模板（复制即用）

### 【工单 A · 建站】

```
你是「外部 Wiki → 本地百科」的条目编写学徒。
任务：按《外部Wiki手动遍历工作流》第四节完成建站。
本地目录：{{wiki目录}}/
源站配置：用户已提供 / 见下方 _源站配置 填写内容。
交付：目录结构 + _crawl 空账本 + seed/queue 初值 + _交接/工单A建站.md
禁区：不爬虫、不存密码、不改配置结构乱填。
```

### 【工单 B · 批次遍历】

```
你是 {{wiki目录}} 的条目编写学徒。本批任务：按手册第五节处理 queue 中最多 {{N}} 个词条。

开工必读：
1. {{wiki目录}}/_工作手册.md
2. {{wiki目录}}/_源站配置.md
3. {{wiki目录}}/PROGRESS.md
4. {{wiki目录}}/_crawl/done.md 与 queue.md

回复开头复述：本批 URL 列表、五条铁则、字数配额、防重规则。

获取正文方式：【粘贴 / 浏览器 / 本地文件】（三选一，用户指定）
【若粘贴】以下是第 1 个词条正文：
---
（用户粘贴）
---

硬性要求：
- 打开前查 done；完成后立刻写 done + url-map + 更新 queue
- 落页形态对齐轮回 wiki（front-matter、分类目录、经历五要素、能力逐条）
- 只写源页有的信息；新发现链接只入队不超额深挖
- 交付：docs 改动 + _crawl 账本更新 + _交接/B0X-….md + PROGRESS

禁区：账号密码、爬虫脚本、黑名单栏目、训练记忆补设定。
```

### 【工单 C · 断点续跑】

```
接上次批次 B0X。先 Read done.md / queue.md / 最近交接单。
跳过一切已在 done 的 URL。
从 queue 顶部未完成项继续，规则同工单 B。
若上次有 fail「需登录」，等用户粘贴正文后再处理，不要空跑。
```

---

## 十、推荐节奏（实操）

| 阶段 | 做什么 | 产出 |
|---|---|---|
| 第 0 天 | 你登录源站；填 `_源站配置`；跑工单 A；播种 1 个总览+角色列表 | 空站+queue |
| 每批 20~40 分钟 | 工单 B：5~10 词条 | 若干 md + done 增长 |
| 每 5 批 | 师傅抽查 2 个 A 级页 + 抽 3 条 done 回源核对 | 返工清单 |
| 收尾 | queue 清空或只剩 skip/fail；写总览索引；可选 mkdocs build | 可浏览本地百科 |

---

## 十一、最小可跑示例（示意）

假设源站有「角色列表」页链出 3 人：甲、乙、丙。

1. queue 初始：`列表页, 甲, 乙, 丙`  
2. 批 B01：处理列表页 → 不建人物，只确认链接已在 queue；列表页记 done（分类：总览）  
3. 批 B01 续：处理甲 → `docs/人物/甲.md` → done  
4. 批 B02：乙、丙…  
5. 打开乙页时发现链到「丁」→ 丁入 queue 末尾，本批若已满则下批再抓  
6. 任何时候重启：只看 queue 顶 + done，不会重复写甲  

---

**一句话口令（贴在对话框置顶）：**  
「先查 `_crawl/done`，再打开源页；摘净正文；写成轮回 wiki 式 md；立刻登记 done；新链只入队。」
