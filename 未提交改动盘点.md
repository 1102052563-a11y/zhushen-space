# 未提交改动盘点（VS Code「101 changed files」）

> 生成日期：2026-06-17 ｜ 分支：`feat/abyss-dungeon-m1` ｜ 对比基准：当前 `HEAD`（工作区未提交改动）

## 一句话总览

这 ~101 项**全部是「已写好但还没 `git commit`」的工作区改动**（不是已提交的内容）。深渊地牢已经在本分支提交了（见 `git log`），但工作区里还**压着一大批跨多个功能的未提交积压**——主要是今天（2026-06-17）陆续做完的 **赌坊 / 技能树 / 欢愉宫世界书 / 进阶点移除→世界结算 / HP·EP 换皮换肤 / 冒险团加入 / NPC 机械生成六维 / 战斗系统增强** 等几大块。

> 项目惯例是**你本人负责 commit + push**（CLAUDE.md），所以这些改动一直没进版本库，越攒越多，最后在 VS Code 里堆成了「101 changed files」。

**数量核对**：`git diff HEAD` 跟踪改动 **70 个**（含 1 个删除）+ 未跟踪新文件 **32 个** = **102**。VS Code 显示 101（少算的 1 个是 `tools/nai-batch/` 下的运行日志）。提示里那一堆 `LF will be replaced by CRLF` **不是改动**，是 Git 换行符警告，可忽略。

---

## 总览表（按功能分组）

| # | 功能块 | 性质 | 文件数 | 状态 |
|---|--------|------|:---:|------|
| A | 🎰 轮回赌坊（Casino） | 新功能 | ~12 | 已落地验证 |
| B | 🌳 职业技能树 / 星图（Skill Tree） | 新功能 | ~6 | 已落地验证 |
| C | 💗 欢愉宫世界书（Joy World Books） | 新功能 | ~13 | 已落地验证 |
| D | ⚙️ 进阶点移除 → 世界结算（World Settlement） | 重构 | 预设全量重导 + 多文件 | build 过 |
| E | 🎨 HP/EP 自定义称呼 + 20 款血条皮肤 | 新功能 | ~6 | build + preview 验证 |
| F | 🤝 冒险团：加入契约者 NPC 的团 | 功能扩展 | ~7 | 已落地 |
| G | 🎲 NPC 机械生成六维 / 生物强度 | 重构 | ~4 | build 过 |
| H | ⚔️ 战斗系统 Layer2/3 增强 | 功能扩展 | ~4 | 已落地验证 |
| I | 💎 强化/宝石、频道、回合洞察等零散接线 | 杂项 | ~12 | 随上面各块连带 |
| J | 🛠 预设/世界书/工具/构建配置 | 资产/工具 | ~14 | — |
| (D2) | 🕳 深渊地牢 | **大部分已提交** | 仅 `abyssEngine.ts`(+21) + App 接线未提交 | M1 已提交 |

---

## A. 🎰 轮回赌坊（Casino）— 新功能

仅主神空间、双币（乐园币→筹码 / 魂币→魂筹贵宾厅）的纯前端确定性赌博，仿强化系统范式。5 个玩法：猜大小骰宝 / 轮回转盘 / 夺命 21 点 / 翻倍梯子 / 角斗场对赌；+ 命运福袋扭蛋 + 魂赌剧情局 + 荷官立绘吐槽 + 战绩→称号成就。

**新文件（11）**
- 组件：`CasinoPanel.tsx`(主面板) · `CasinoGladiator.tsx`(角斗场) · `CasinoGacha.tsx`(扭蛋) · `CasinoSoul.tsx`(魂赌) · `CasinoManager.tsx`(设置/API/限红/荷官)
- store：`store/casinoStore.ts`
- 引擎：`systems/casinoEngine.ts` · `casinoGacha.ts` · `casinoSoul.ts` · `casinoHonors.ts`(战绩称号) · `casinoDealers.ts`(荷官立绘)

**连带改动**
- `promptRules.ts` 新增：`GLADIATOR_MATCH_RULE` `GLADIATOR_BATTLE_RULE` `GACHA_REWARD_RULE` `SOUL_GAMBLE_RULE` `CASINO_BANTER_RULE`
- `App.tsx`：`import CasinoPanel` + `useCasino` + `computeGladiatorOdds` 等接线
- `VariableManager.tsx`：🎰 赌场启动按钮 ｜ `SettingsPanel.tsx`：挂 `CasinoManager`
- 世界书 `世界书/赌场/战斗写作指导.json`(角斗场战斗叙事指导)

---

## B. 🌳 职业技能树 / 星图（Skill Tree）— 新功能

右导航玩家面板 + 设置可视化编辑器，花「潜能点」（纯确定性，无 AI 给点）解锁星图节点→灌进技能/天赋。内置剑士星图 33 节点 4 分支，仅 B1。

**新文件（5）**
- 组件：`SkillTreePanel.tsx`(玩家🌳) · `TreeCanvas.tsx`(共享 SVG) · `SkillTreeManager.tsx`(编辑器)
- store：`store/skillTreeStore.ts`（`drpg-skilltree`）
- 系统：`systems/skillTree.ts`（确定性结算/校验）

**连带改动**
- `promptRules.ts` 新增：`POTENTIAL_POINT_RULE` `SKILL_UPGRADE_PROMPT` `STARCORE_GEN_PROMPT`(星核) `CONSTELLATION_AWAKEN_PROMPT`(星座觉醒) `SKILLTREE_GEN_PROMPT`(星图设计)
- `App.tsx`：`import SkillTreePanel` + `useSkillTree` + `SKILLTREE_TUNING`
- `docs/CODE_MAP.md`：补技能树定位条目 ｜ `VariableManager.tsx`：🌳 技能树按钮 ｜ `SettingsPanel.tsx`：挂 `SkillTreeManager`

---

## C. 💗 欢愉宫世界书（Joy World Books）— 新功能

欢愉宫专用世界书：内置 5 本（蓝灯常驻/绿灯关键词 onJoySend 注入），可导入/开关/编辑/导出；包间加 🤸 姿势 / ⛓ BDSM 快捷按钮（从世界书提标题填输入框由玩家发）。

**新文件（~13）**
- 内置库：`public/joy-worldbooks/`：`manifest.json` + `wb0~wb4.json`（6）
- 系统：`systems/joyWorldBook.ts`（`buildJoyWbInjection` 注入构造）
- 源文件：`世界书/欢愉宫/` 下 5 本世界书 JSON（真实性 NSFW 指导 / BDSM / 性爱姿势 / 中日式淫语 / 女性服装肉体描写）

**连带改动**
- `App.tsx`：`useJoy` + `hydrateJoyWorldBooks` + `buildJoyWbInjection`
- `JoyManager.tsx`(+156 导入/开关/编辑/导出) · `JoyPanel.tsx`(+93 姿势/BDSM 按钮) · `store/joyStore.ts`(+110) · `systems/joyGirls.ts`
- `promptRules.ts` 新增 `JOY_PRIVATE_FIELDS_RULE`(私密信息全量维护)
- `vite.config.ts`：joy-worldbooks 自动 manifest 插件

---

## D. ⚙️ 进阶点移除 → 世界结算（World Settlement）— 重构

**进阶点数 + 击杀结算彻底移除**，升级改「世界结算」：关键词 `【结算任务】`→正文 AI 按 `WORLD_SETTLEMENT_RULE` 判主线 + 按世界之源% 评级（E~SSS）发奖→`<世界结算>` 渲染成金边结算卡→末尾 `<state>` 真入账。配套货币扩到 4 种（+技能点/黄金技能点）。

**核心改动**
- **删除**：`systems/advancePoints.ts`（-204，整文件移除 `parseKillManifest`/`buildKillSettlement`）
- `App.tsx`：去掉 `advancePoints` 引用，新增 `<世界结算>` 渲染卡 + `WORLD_SETTLEMENT_RULE`
- `promptRules.ts`：`WORLD_SETTLEMENT_RULE`（+ 任务评分 `QUEST_RATING_RULE`）
- `WorldSelector.tsx`：🌍 选世界旁 📊「结算任务」按钮 ｜ `MiscPanel.tsx`：评级徽章
- **预设全量重导**（见下方 J 区）：`item/npc/player.json` 及其源 `物品管理/NPC演化/主角演化.json` 删掉 `进阶点数 ap.<ID>` 相关规则、改 4 币

---

## E. 🎨 HP/EP 自定义称呼 + 20 款血条皮肤 — 新功能

> 已写进 `更新内容.md`（本批唯一更新到 changelog 的功能）。

让特殊角色的 HP/EP 条显示专属称呼（如血池/血怒）+ 20 款血条皮肤换肤。**最高铁则：自定义名「只出不进」**——只渲染 + 喂 AI 措辞，永不进解析器/正则；机器通道（`hp.B1` 指令 + 状态行）永远写死 HP/EP 原词。

**改动（与 changelog 一致）**
- `store/playerStore.ts`：`PlayerProfile` 加 `hpLabel?`/`epLabel?`/`barStyle?`
- `components/Bar.tsx`：加 `styleId`/`kind` props + 导出 `BAR_STYLES`（+45）
- `PlayerSidebar.tsx`：血条下「✎ 自定义血条」面板（输入称呼 + 皮肤网格）+ 点血条展开（+112）
- `src/index.css`：`.barfill` + `bf-<style>-hp/-ep` 多层皮肤 + 告警闪烁（+189）
- `structuredRecall.ts` / `promptRules.ts`：仅给 AI 一句别名提示

---

## F. 🤝 冒险团：加入契约者 NPC 的团 — 功能扩展

主角可加入契约者 NPC 的冒险团（主角非团长）：NPC 加 `affiliatedTeam` 字段 + 私聊表达意愿→NPC 同意发 `<加入冒险团>` 信号→弹窗→`generateJoinedTeam` 全量生成。

**新文件**：`systems/adventureTeamGen.ts`（入团全量生成）

**连带改动**
- `store/npcStore.ts`：加 `affiliatedTeam?`/`unitType?` 字段 + `isNpcId` 校验（+53）
- `store/adventureTeamStore.ts`(+74) · `AdventureTeamPanel.tsx`(+45) · `NpcChatPanel.tsx`(+76) · `npcChatStore.ts` · `systems/npcChat.ts`(+39)
- `promptRules.ts` 新增 `NPC_TEAM_JOIN_CHAT_RULE` `NPC_TEAM_AFFILIATION_RULE`

---

## G. 🎲 NPC 机械生成六维 / 生物强度 — 重构

NPC 六维改前端纯机械生成（治 AI 幻觉）：AI 只给 `genAttrs` 元字段→前端反推六维（注水分配 + 形态压制 + 种子随机 + 闭环自检）。

**改动**
- `systems/npcAttrGen.ts`(+282 核心逆运算) · `systems/bioStrength.ts`(+40) · `systems/derivedStats.ts`(+90)
- `NpcDetail.tsx`：🎲「机械生成六维」按钮（+139）
- `promptRules.ts` 新增 `NPC_ID_RULE` `NPC_ENTRY_BIO_RULE`

---

## H. ⚔️ 战斗系统 Layer2/3 增强 — 功能扩展

buff/debuff/DoT·HoT/护盾/冷却/群攻/控制 + 蓄力大招 + 领域/阵法 + 不死/锁血。

**改动**：`systems/combatEngine.ts`(+130) · `CombatPanel.tsx`(+72) · `store/combatStore.ts` · `CombatManager.tsx`

---

## I. 💎 杂项接线（随上面各块连带）

| 文件 | 用途 |
|------|------|
| `systems/attrBonus.ts` (+9) | 宝石/队伍属性增量（`withAttrDelta`） |
| `EnhancePanel.tsx` (+12) | 强化/宝石子系统 |
| `systems/channelTrade.ts` (+33) · `ChannelPanel.tsx` (+25) | 公共频道交易 |
| `TurnInsightPanel.tsx` (+111) · `turnInsightStore.ts` | 回合洞察：HP/EP 上限增减着色 ↑/↓（`VitalDelta`） |
| `systems/structuredRecall.ts` · `stateParser.ts` · `miscParser.ts` | 召回/指令解析适配新字段 |
| `BackpackModal.tsx` · `CharEditForms.tsx` · `CharacterCreation.tsx` · `DicePanel.tsx` · `OnScenePanel.tsx` | 各面板小适配 |
| `store/`：`characterStore` `channelStore` `combatStore` `creationTemplateStore` `itemStore` `miscStore` `settingsStore` | 字段/action 小改 |
| `systems/saveManager.ts` (+9) · `configExport.ts` (+24) | 把新 store（赌坊/技能树）纳入存档快照 + 配置导出 |
| `data/channelDefaultPreset.json` · `teamDefaultPreset.json` | 默认预设小调 |

---

## J. 🛠 预设 / 世界书 / 工具 / 构建（资产类）

**预设 JSON 大改 = 全量重导（行数虚高）**：`item/npc/player.json`（public）及其源 `物品管理/NPC演化/主角演化.json`（演化预设）显示几百上千行 diff，但**大部分是格式重排**（旧的 PowerShell `ConvertTo-Json` 缩进 → 标准缩进），**真实内容改动**集中在：① 删进阶点 `ap.<ID>` 规则；② 4 币（技能点/黄金技能点）；③ 新功能配套提示词。**不是逐条手改，是重新导出**。

| 文件 | 说明 |
|------|------|
| `正文预设/轮回乐园-Claude.json` / `-Gemini.json` (各+85) | 主正文预设：世界结算/技能点等规则 |
| `public/presets/novel.json` (+42) · `世界书/轮回乐园小说.json` (+42) | 正文/小说世界书 |
| `世界书/ST_WI_Modular_Output_v11…json` (+571) | 模块化输出世界书 |
| `public/presets/modular-output.json` (+1) | 模块化输出（极小） |
| `tools/nai-batch/jobs.json` (+393) | NAI 批量生图任务配置 |
| `tools/nai-batch/_run.log` / `_run.err` | NAI 运行日志（产物，建议 gitignore 或不提交） |
| `vite.config.ts` (+56) | joy-worldbooks / portraits 自动 manifest 插件 |

---

## 🕳 关于深渊地牢（澄清）

深渊地牢 **M1~M4 主体已经提交在本分支**（`git log` 可见 5 个 commit）。所以它**基本不在这 101 项未提交里**——未提交的只有：`systems/abyssEngine.ts`(+21 小调) + `App.tsx` 里那段深渊接线（`AbyssPanel`/`abyssPrompts`/`abyssData` 的 import）。换句话说深渊的组件/store/数据/设计文档都已入库，仅剩零星收尾未提交。

---

## ✅ 建议的提交分组（如果要分批 commit）

这 101 项混了 8 个功能，建议**按功能拆成多个 commit**，别一坨：

1. `feat(赌坊): 轮回赌坊 5 玩法 + 扭蛋/魂赌/荷官` — A 区全部
2. `feat(技能树): 职业星图 + 潜能点 + 编辑器` — B 区全部
3. `feat(欢愉宫): 专用世界书 + 姿势/BDSM 快捷` — C 区全部
4. `refactor(升级): 移除进阶点/击杀结算 → 世界结算 + 4 币` — D 区 + 预设重导
5. `feat(血条): HP/EP 自定义称呼 + 20 款皮肤` — E 区
6. `feat(冒险团): 加入契约者 NPC 的团` — F 区
7. `refactor(NPC): 机械生成六维 / 生物强度` — G 区
8. `feat(战斗): Layer2/3 增强` — H 区
9. 杂项接线（I/J）随相关功能 commit 一起带，或单独 `chore` 收尾

> `tools/nai-batch/_run.log` / `_run.err` 是运行产物，建议加进 `.gitignore` 而不是提交。
