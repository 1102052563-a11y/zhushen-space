# 全动漫作品 Wiki 人工采集工作流

> 目标：把**任意动漫作品**的外站 Wiki 页面，在具备完整转载授权时**逐字归档**为结构化本地资料库。AI 不改写、不概括、不补充设定，只做作品隔离、原文存放、分类、链接和来源记录。适用动画、漫画、轻小说改编、游戏改编及其系列/续作；不是《轮回乐园》专用流程。
>
> 核心原则：**一部作品（或一个明确的系列宇宙）一套独立资料库、一份独立来源台账、一条独立队列。** 不得把不同作品的人物、势力、设定、时间线或“已完成”记录混在一起。

---

## 1. 使用边界

- 用户自行在浏览器完成登录、二次验证和验证码；AI 只阅读当前已授权会话中正常可见的页面。
- 不索要、不读取、不保存账号、密码、Cookie、token、验证码、私人消息或其他敏感信息。
- 不绕过付费墙、访问限制、robots 规则、站点条款或频率限制；不使用爬虫、批量请求、自动翻页和脚本抓取。
- **只允许视觉采集**：AI 仅根据用户浏览器呈现的页面截图进行图像识别（OCR）。禁止读取或请求 HTML、DOM、网页源代码、开发者工具内容、接口响应、站点 API、RSS、导出数据、浏览器自动化文本或任何非视觉页面数据。
- 以“打开一页 -> 截一屏 -> OCR 一屏 -> 人工核对一屏 -> 向下滚动”的人工节奏执行；不自动滚动，不跳过可见正文区域。
- **全文归档必须先有权利依据**：只有用户是著作权人/权利人、已获得明确转载许可，或页面以允许完整复制与再发布的开放许可证发布时，才可逐字保存页面正文。登录可见不等于有复制权。
- 权利依据不明、禁止转载、只允许浏览、受付费/会员限制或许可证不允许完整再发布时，登记 `blocked_rights`；仅记录 URL、标题、许可状态和分类，不保存正文，也不由 AI 改写、概括或补充。
- 在获准全文归档的页面中，正文字符、标点、段落、标题、表格文字、脚注文字必须保持原样；AI 只能补充文件 front-matter、来源卡和分类索引，且这些元数据必须与原文分离。

## 2. 作品隔离与目录

为每部作品创建独立目录。系列作品只有在用户明确指定“共享世界观/同一宇宙”时才允许共用资料库。

```text
动漫百科库/
├─ 作品注册表.csv
├─ 鬼灭之刃/
│  ├─ _源站配置.md
│  ├─ _采集台账.csv
│  ├─ _队列.csv
│  ├─ _原文/                 ← 仅存获准全文转载的逐字页面副本
│  ├─ _截图/                 ← 原文证据：按页面与屏幕序号保存的浏览器截图
│  ├─ _交接/
│  └─ docs/
│     ├─ 总览/ 剧情/ 人物/ 势力/ 地点/ 物品/ 设定/
├─ 进击的巨人/
│  └─ ...
└─ Fate系列/
   └─ ...
```

### 2.1 作品注册表

根目录 `作品注册表.csv` 一作品一行，用来防止把同一作品建成多个库：

```csv
work_id,work_title,aliases,franchise,local_root,primary_language,status,created_at,note
kimetsu-no-yaiba,鬼灭之刃,鬼杀队|Demon Slayer,鬼灭之刃,动漫百科库/鬼灭之刃,zh-CN,active,2026-07-25,TV动画与漫画共用需在来源中标版本
```

建库前按 `work_id`、正式名和别名检查。已存在时继续原目录；同名但不同作品必须使用唯一 `work_id`，例如 `fate-stay-night` 与 `fate-zero`。

### 2.2 源站配置

每部作品的 `_源站配置.md` 由用户或审核者先填写：

```markdown
# 源站配置

- 作品：<正式名>
- work_id：<唯一英文 slug>
- 收录版本：<TV动画第几季 / 漫画 / 轻小说 / 游戏；不明则写 unknown>
- 源站名称：<Wiki 名称>
- 入口 URL：<作品首页或分类页>
- 访问身份：用户已在可截图的浏览器登录 / 无需登录
- 允许栏目：人物、势力、地点、物品、设定、剧情、集数/章节梗概
- 禁止栏目：讨论、编辑历史、用户页、管理页、论坛、广告、外链、隐私页
- 允许跨站：否（默认）；若允许，列出白名单域名
- 剧透规则：全剧透 / 仅动画已播部分 / 截止 <季/集/章>
- 单批上限：5~10 个普通页，或 3~5 个长页
- 全文归档模式：仅在已确认授权后启用 / 仅元数据（默认）
- 权利依据：<权利人授权书 / 页面许可证 URL 与版本 / 用户自有内容；未知则 unknown>
- 许可证要求：<是否允许复制、修改、再发布、署名要求、相同方式共享要求>
- 版权/引用限制：<未获得全文转载许可时必须 blocked_rights>
```

同一作品有漫画、动画、轻小说等不同版本时，所有事实必须标注版本。例如“动画第一季第 19 集”与“漫画第 83 话”不能互相替代。

## 3. 采集台账与去重

每个作品目录有独立 `_采集台账.csv`。它是该作品唯一的完成记录，绝不存放登录信息。

```csv
canonical_url,source_page_id,source_title,version_scope,content_type,status,priority,discovered_from,aliases,source_revision_or_date,accessed_at,last_checked_at,rights_basis,license_url,license_requirements,capture_mode,screenshot_manifest,ocr_review_status,verbatim_file,verbatim_sha256,char_count,destination_files,change_note,duplicate_of,reviewer
```

### 3.1 去重规则

打开新页面前，按此顺序检查本作品台账：

1. `canonical_url`：去掉 `#锚点`、`utm_*` 等跟踪参数、统一协议和尾部斜杠后的 URL。
2. `source_page_id`：站点提供的稳定条目 ID 或永久链接 ID；有则优先使用。
3. `source_title + version_scope`：URL 改版或没有 ID 时的后备键。
4. `aliases`：重定向页、别名、译名和旧 URL；都映射回同一主记录。

满足以下条件才算完成：`status=completed`，且 URL、页面标题、访问时间、权利依据、许可证要求、采集方式、截图清单、OCR 核对状态、逐字原文文件、原文 SHA-256、字符数和目标分类文件均已填写。

- 同一规范 URL 且来源版本未变：跳过，只更新 `last_checked_at`。
- 同一页版本更新：改为 `needs_review`，只增量处理变化内容。
- 重定向、镜像、同页锚点：标记 `duplicate`，填写 `duplicate_of`，不重复建页。
- 同名但不同动漫：不在本作品台账中判重，必须回 `作品注册表.csv` 确认归属。

### 3.2 状态

| 状态 | 含义 |
|---|---|
| `queued` | 已发现，待阅读 |
| `in_progress` | 正在阅读或整理 |
| `completed` | 已按授权逐字归档，权利与文件校验信息完整 |
| `no_action` | 已读，但不属于允许栏目或没有可归档正文 |
| `needs_review` | 版本归属、分类或来源页面版本待确认 |
| `blocked_login` | 用户尚未完成登录或无访问权限 |
| `blocked_access` | 验证码、频率限制、付费/条款限制等，必须停止 |
| `blocked_rights` | 无完整转载权利依据，禁止保存或改写正文 |
| `blocked_visual` | 无法获得用户浏览器的清晰页面截图；不得退回 HTML/DOM/API 等直接读取方式 |
| `duplicate` | 重定向、镜像或同页别名 |
| `out_of_scope` | 超出作品、版本、栏目或剧透边界 |
| `error` | 页面失效或无法正常读取 |

### 3.3 登记时机

1. 在入口/分类页发现链接时，先登记 `queued`。
2. 打开页面前改为 `in_progress`。
3. 只有确认权利、逐字保存原文、写入校验值并完成分类后，才能改为 `completed`。
4. 每批结束不得遗留无说明的 `in_progress`；未做完则写进度并改回 `queued` 或 `needs_review`。
5. `blocked_*`、`duplicate`、`out_of_scope` 记录永不删除，避免反复访问同一页面。

## 4. 每页人工处理流程

### 步 1：从有限入口建队列

只从源站配置指定的入口、分类、目录、导航模板和已确认相关页取链接。先过白名单/黑名单和作品归属检查，再写入队列。不得无限扩展相关链接。

推荐优先级：

1. P0：作品总览、主角、核心反派、主势力、基础世界观、关键时间线。
2. P1：重要配角、主要地点、核心能力/系统、关键道具。
3. P2：单集人物、支线势力、普通物品。
4. P3：仅提及条目、纯索引、低信息页。

### 步 2：检查视觉可访问性

- 用户浏览器可正常显示页面，且可取得清晰截图：记录浏览器地址栏可见 URL、页面可见标题和截图尺寸。所有来源信息都必须来自可见画面。
- 跳到登录页：登记 `blocked_login`，请用户自行在浏览器处理后再试。
- 验证码、访问受限、订阅限制或条款不允许：登记 `blocked_access`，不尝试规避。
- 没有浏览器截图、截图模糊、文字被遮挡、无法覆盖正文，或只能取得网页代码/接口数据：登记 `blocked_visual`，停止本页。不得以 HTML、DOM、阅读模式文本、API 或开发者工具替代截图。

### 步 3：权利检查与原文档案卡

在 OCR 和保存正文前，先通过版权/许可证页面的**浏览器截图**，或用户提供的书面许可，确认允许**完整复制及本地再发布**。不得通过源码、接口或网页抓取读取许可证。权利不确定即 `blocked_rights`。

获准后按视觉顺序截取页面：从标题和正文第一屏开始，屏与屏之间保留一到两行重叠文字，直至正文、脚注和许可声明结束。每屏 OCR 后与截图逐字核对，不清晰处必须重截图，不可猜字。然后填写档案卡：

```markdown
## 来源页原文档案卡
- 作品 / 版本：<work_id> / <version_scope>
- 页面：<标题>
- URL：<canonical_url>
- 页面版本：<修订号、最后编辑日期或 unknown>
- 权利依据：<授权书编号 / 许可证名称、链接与版本 / 用户自有内容>
- 许可证要求：<署名/链接/相同方式共享/不得修改等>
- 原文文件：_原文/<页面稳定名>.md
- 原文校验：SHA-256 <哈希>；字符数 <数量>
- 截图清单：_截图/<页面稳定名>/manifest.csv
- OCR 核对：逐屏人工核对完成 / 待重截第 <N> 屏
- 分类：人物 / 势力 / 地点 / 物品 / 设定 / 剧情 / 总览
- 目标分类页：docs/<分类>/<页面稳定名>.md
```

AI 不判断事实真伪、不抽取观点、不删除推测、不修正错别字、不合并段落，也不混入评论区或站点外内容。原文仅能依据截图中可见且在授权范围内的正文按视觉顺序保存；讨论区、广告、用户资料、编辑控件等禁止栏目仍不归档。

每个页面的 `_截图/<页面稳定名>/manifest.csv` 格式如下：

```csv
screen_no,image_file,viewport_note,overlap_with_previous,ocr_checked,issue
001,001.png,标题和正文起始,none,yes,
002,002.png,正文续段,末尾两行与001重叠,yes,
```

### 步 4：逐字保存并创建分类入口

原文优先写入 `_原文/<页面稳定名>.md`。该文件只能由经过逐屏人工核对的 OCR 结果按截图顺序拼接，不能用 AI 重写。允许的变化仅限于：文件顶部增加与正文分隔的 YAML front-matter，以及为满足文件格式而使用等义的换行编码；正文内容、标题层级、段落顺序、表格单元格和脚注文字均不得改变。

`docs/` 下的分类页不是二次创作页面，而是只含元数据和原文链接的入口。不得复制、摘要、改写或拆散原文。例如：

```markdown
---
title: <源页面标题>
作品: <作品正式名>
版本: <version_scope>
分类: 人物
原文来源: <canonical_url>
权利依据: <许可证或授权>
原文校验: sha256:<哈希>
---

# <源页面标题>

- 原文归档：[查看逐字原文](../../_原文/<页面稳定名>.md)
- 来源页面：<canonical_url>
- 访问日期：YYYY-MM-DD
- 分类：人物
```

每个来源页对应一个原文文件；不同分类需要引用同一页面时，只创建额外入口链接，绝不复制原文第二份。

| 内容 | 推荐目录 | 分类入口允许内容 |
|---|---|---|
| 人物 | `docs/人物/` | 标题、作品/版本、来源、权利信息、原文链接 |
| 势力 | `docs/势力/` | 标题、作品/版本、来源、权利信息、原文链接 |
| 地点 | `docs/地点/` | 标题、作品/版本、来源、权利信息、原文链接 |
| 物品 | `docs/物品/` | 标题、作品/版本、来源、权利信息、原文链接 |
| 世界观/能力/术语 | `docs/设定/` | 标题、作品/版本、来源、权利信息、原文链接 |
| 剧情/集数/章节 | `docs/剧情/` | 标题、作品/版本、来源、权利信息、原文链接 |

分类入口 front-matter 最小模板：

```yaml
---
title: <正式名>
作品: <作品正式名>
版本: <动画/漫画/轻小说范围>
分类: 人物
来源: <canonical_url>
权利依据: <许可证或授权>
原文文件: <相对 _原文 路径>
原文校验: sha256:<哈希>
首次归档批次: <批次号>
---
```

### 步 5：校验、署名与来源

完成原文保存后，计算文件 SHA-256 和字符数，写入台账与分类入口。许可证要求署名、原链接、许可文本或相同方式共享时，必须完整执行。页面底部可使用：

```markdown
## 来源与授权
- <源站名称>：《<页面标题>》<canonical_url>（访问于 YYYY-MM-DD；版本：<version_scope>）。
- 权利依据：<许可证名称/版本/授权书标识>；<许可证链接>。
- 视觉证据：`_截图/<页面稳定名>/manifest.csv`（逐屏 OCR 核对完成）。
- 原文校验：SHA-256 `<哈希>`；字符数：<数量>。
```

不得补写内容、替换敏感词、纠正错别字、清理广告以外的原文段落，或把外站标注转换成 AI 判断。原文必须如实保留；只有在权利限制要求删除/更正时，重新归档并在台账注明原因。

### 步 6：处理冲突

以下情况进入 `needs_review`：人物同名但身份不明、动画与漫画版本不一致、分类无法确定、来源页面版本发生变化或内容超出设置的剧透边界。无完整转载权利依据时不进入审核队列，直接 `blocked_rights`。

```text
status：needs_review
change_note：<动画/漫画/不同来源>在 <字段> 上冲突
summary：<待确认的作品/版本/分类/页面变更>
destination_files：none 或 <仅分类入口>
```

分类无法确定时，原文可先归入 `_原文/未分类/`，台账标 `needs_review`；未经权利确认不得保存该原文。

### 步 7：闭环

更新台账的权利依据、许可证链接、截图清单、OCR 核对状态、原文路径、SHA-256、字符数、目标分类入口和变更说明，将 `in_progress` 改为最终状态。当前页新发现的站内相关链接，只有符合范围且未登记时才加入 `queued`。

## 5. 原文保真标准

- 归档正文与页面原文逐字一致：不改写、不摘要、不翻译、不补全、不删节、不纠错、不重排。
- AI 只做文件命名、作品/版本归属、分类入口、原文链接、权利信息、哈希和台账状态等机械整理。
- 原文只能来自截图 OCR：每一屏必须有对应截图和 `manifest.csv` 行；相邻屏必须有可见重叠行用于验证顺序；OCR 不确定字必须重截，不能按上下文猜测。
- 禁止以任何直接网页读取方式补全 OCR：不读 HTML、DOM、源代码、接口、API、RSS、导出文件、浏览器文本提取或开发者工具。
- 来源页面修订后，保留旧归档版本并以新文件归档新版本；台账记录旧/新修订号、归档时间和替代关系，禁止覆盖旧版。
- 分类无法确认时放 `_原文/未分类/`，而不是基于内容推测分类。
- 同一世界观的跨作关系必须先在 `作品注册表.csv` 标明；未确认前，默认不同作品独立。
- 每个原文文件和分类入口写明作品与版本，防止漫画、TV 动画、剧场版、重制版混写。

## 6. 批次交接与验收

每批创建 `_交接/<日期>-<批次>.md`：

```markdown
# 动漫 Wiki 采集交接单 · <作品> · <批次>

## 范围
- work_id / 版本：
- 源站入口：
- 本批页面上限 / 实际处理数：

## 台账结果
| 状态 | 数量 | 说明 |
|---|---:|---|
| completed | 0 | 已获授权并逐字归档 |
| no_action | 0 | |
| needs_review | 0 | |
| blocked_login / blocked_access / blocked_rights / blocked_visual | 0 | |
| duplicate / out_of_scope / error | 0 | |

## 本地更新
| 来源页 | 原文文件 | 分类入口 | 版本 | 权利依据 / SHA-256 |
|---|---|---|---|---|

## 待裁定事项
| 页面 | 问题 | 影响条目 | 建议 |
|---|---|---|---|

## 下批续接
- 下一个 queued URL：
- 队列余量：
- 是否达到停止条件：是 / 否
```

验收前逐项确认：

1. 没有未说明的 `in_progress` 行。
2. 每个 `completed` 行都有 URL、标题、访问时间、权利依据、许可证要求、截图清单、OCR 核对状态、原文文件、SHA-256、字符数和分类入口。
3. 同一作品内没有重复规范 URL 的多个 `completed` 行。
4. 已抽查原文文件和截图清单：每段原文可追溯到截图，正文没有 AI 改写、摘要、删节或跨页拼接；新增内容未跨作品、未跨版本混写。
5. 文档、台账、交接单没有密码、Cookie、token、验证码或个人资料。
6. 运行本地百科对应的链接/构建校验；构建产物不作为源文件维护。

## 7. 采集 AI 工单模板

```text
你在执行“全动漫作品 Wiki 人工采集”。目标作品：<作品名>，work_id：<id>，版本范围：<动画/漫画/轻小说/截止集数或全剧透>。

先读取本作品的 _源站配置.md、_采集台账.csv、_队列.csv 和本工作流。用户已自行在浏览器登录；你不得要求、读取或保存任何账号、密码、Cookie、token、验证码，也不得绕过限制或使用爬虫/批量请求。只可使用浏览器视觉截图和图像 OCR，禁止读取 HTML、DOM、源代码、接口、API、RSS、导出内容、自动化文本或开发者工具。

本批最多处理 <N> 页。每个页面严格执行：先按地址栏可见 URL、页面视觉标题和版本判重；登记 queued -> in_progress -> 最终状态；先确认用户权利、明确许可或开放许可证是否允许完整复制与再发布。没有明确权利依据时标 blocked_rights，不保存、摘要、改写或补充正文。获准时从顶部开始逐屏截图，保留相邻屏重叠行；逐屏 OCR 并人工核对，模糊字重截，不猜测；保存截图清单后才将 OCR 原文逐字存入 _原文，计算 SHA-256 和字符数，再按人物/势力/地点/物品/设定/剧情创建只含元数据与原文链接的分类入口。不得改写、概括、翻译、纠错、删节或跨页面拼接；没有截图时标 blocked_visual，不得退回 HTML/DOM/API 方式。不同动漫作品、不同改编版本不得混写；分类无法确认时标 needs_review。

完成后交付：本批本地 Markdown、更新后的 _采集台账.csv、_队列.csv 和 _交接/<批次>.md，并汇总 completed / needs_review / blocked 数量。
```
