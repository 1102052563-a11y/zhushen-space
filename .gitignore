# ───── 本地工具 / 临时文件（不入库）─────
.claude/
*.tmp
# 表情包压缩用的临时 gifsicle 安装目录（scratch，可随时删）
/_sticker_tools/

# ───── 依赖与构建产物（Cloudflare 会自己 npm install + 构建）─────
node_modules/
dist/
# 肖像图库清单：由 vite 插件按 public/portraits/ 子文件夹自动生成，不必入库
zhushen-space/zhushen-space/public/portraits/manifest.json
# 背景音乐：线上走 R2（functions/bgm 从桶提供），本目录不入库——否则构建的空 manifest.json 会作静态资源把 Function 挡住；
# 本地 dev 可往此目录丢音乐文件测试（vite 插件生成 manifest），不影响线上。⚠用 public/bgm 不用 public/audio/bgm（/audio/* 会被静态化）
zhushen-space/zhushen-space/public/bgm/
# 轮回WIKI 构建产物：由 vite 插件 build-wiki 每次 build 时用 mkdocs 自动重建（Cloudflare 同理），不入库；只提交 lunhui-wiki/docs/*.md 源
zhushen-space/zhushen-space/public/wiki/

# ───── 大体积立绘素材（欢愉宫看板娘，走 Cloudflare R2 部署，vite 插件本地同步进 public/joy-girls/；见 tools/upload-r2/R2部署说明.md）─────
/欢愉宫图片/
# 表情包原图（动态奶龙等的高清原件，142MB）——已 gifsicle 压缩进 public/stickers/<包名>/ 入库部署，原件留本地不入库
/动态奶龙/
# 背景音乐原件（1.3GB 游戏OST等）——走 Cloudflare R2 部署（同 欢愉宫图片），git 不入库；见 tools/upload-r2/
/BGM/

# ───── 第三方 / 参考资料（非本项目代码，不入库，保持仓库精简）─────
# 另一个预构建 app，仅作移植参考，不需要部署，也不必备份
fanren-remake-public-assets/
# 下载来的模板，与本项目无关
awesome-design-md-main/

# ───── 系统垃圾 ─────
.DS_Store
Thumbs.db
